#!/usr/bin/mash
# Basic control flow constructs from other langs

val <- 5

if val < 3 {
    run echo "Correct"
}

for i <- 1 .. 3 {
    run echo "Looping: " + i
}

new_func <- fn (param) {
  return param+1
}
